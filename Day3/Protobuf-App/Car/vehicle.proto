syntax = "proto3";

message Location {
    int32 x = 1;   // X-coordinate
    int32 y = 2;   // Y-coordinate
}

message VehicleData {
    Location location = 1; // Nested message for location
    float speed = 2;       // Vehicle speed
    bool brake = 3;        // Brake status 
}